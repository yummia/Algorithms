```java
/*
描述：
  输入一个 int 型正整数，计算出该 int 型数据在内存中存储时 1 的个数。

数据范围：保证在 32 位整型数字范围内

输入描述：
  输入一个整数（int 类型）

输出描述：
  这个数转换成 2 进制后，输出 1 的个数

示例：
  输入：8
  输出：1

*/

import java.util.*;
public class Main(){
    public static void main(String[] args){
        de2bin2();
    }
    public static void de2bin2(){
        int num = sc.nextInt();
	System.out.println(Integer.toString(nux,2).replaceAll("0","").length());
    }
}


```
